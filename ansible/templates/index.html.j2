<!-- 20260729-1210_017 -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Hantt Practical - nginx over HTTPS</title>
    <style>
        body { font-family: system-ui, sans-serif; background: #1a2332; color: #e8edf4;
               display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .card { background: #243146; padding: 3rem 4rem; border-radius: 12px; text-align: center; }
        h1 { color: #5fd68a; margin-bottom: 0.5rem; }
        p { color: #9db2cc; }
        code { background: #1a2332; padding: 2px 8px; border-radius: 4px; }
    </style>
</head>
<body>
    <div class="card">
        <h1>&#x2705; nginx over HTTPS</h1>
        <p>Hantt &amp; Company practical exercise</p>
        <p>Host: <code>{{ ansible_hostname }}</code> &middot; Configured by Ansible</p>
    </div>
</body>
</html>
